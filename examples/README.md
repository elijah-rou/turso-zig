# Examples

Add executable examples after the local API is usable. The first example should
open `:memory:`, bind values into a prepared statement, read typed row values,
and release every resource deterministically.

Do not include authentication tokens, remote URLs, or generated native binaries
in this directory.
